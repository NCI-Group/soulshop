<table class="catalog">

<% @inventories.each do |inventory| %>
  <tr>
    <td>
      <div class="image">
        <%= link_to (image_tag inventory.image_url, :class => "image-style"), inventory %>
      </div>
    </td>
    <td class="product-info">
      <div class="title">
        <%= link_to inventory.Designation, inventory, :class => "title-style"%>
      </div>
      <div class="description">
        <%= inventory.Description %>
      </div>
      <div class="price">
        <%= number_to_currency(inventory.Cost, :unit => "qt.")%>
      </div>
    </td>
    <td class="add-cart">
      <div class="links">
        <% if session[:login] == 1 %>
          <%= link_to 'Edit', edit_item_path(inventory) %>
          <%= link_to 'Destroy', inventory, :confirm => 'Are you sure?', :method => :delete %>
        <% else %>
        <div class="karma">
          Karma Rating: <%= inventory.Karma_Rating %>
        </div>
        </br>
          <a href="/cart/<%= inventory.id %>" onclick="addRemoveItem()">
            <img src="/assets/shopping-cart.png" alt="Product not added" class="cart-image">
            <br/>
            Add to Cart
          </a><br/><br/>
        <% end %>
      </div>
    </td>
  </tr>
<% end %>

</table>

</br>

<p id="notice"><%= notice %></p>

<h1>Inventories Base Layout - TO DELETE</h1>

<table>
  <thead>
    <tr>
      <th>Designation</th>
      <th>Description</th>
      <th>Freshness</th>
      <th>Image url</th>
      <th>Karma rating</th>
      <th>Cost</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @inventories.each do |inventory| %>
      <tr>
        <td><%= inventory.Designation %></td>
        <td><%= inventory.Description %></td>
        <td><%= inventory.Freshness %></td>
        <td><%= inventory.image_url %></td>
        <td><%= inventory.Karma_Rating %></td>
        <td><%= inventory.Cost %></td>
        <td><%= link_to 'Show', inventory %></td>
        <td><%= link_to 'Edit', edit_inventory_path(inventory) %></td>
        <td><%= link_to 'Destroy', inventory, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Inventory', new_inventory_path %>
